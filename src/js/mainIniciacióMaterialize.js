// Iniciación tool tip
$(document).ready(function(){
    $('.tooltipped').tooltip();
  });
  
// Iniciación sidenav
$(document).ready(function(){
    $('.sidenav').sidenav();
  });

  // Iniciación slider
  $('.carousel.carousel-slider').carousel({
    fullWidth: true
  });
  $('.carousel.carousel-slider').carousel({
    fullWidth: true,
    indicators: true
  });
        